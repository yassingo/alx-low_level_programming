nclude <stdlib.h>
#include <stdio.h>
#include "lists.h"

int main(void)
{
    listint_t *head;
    listint_t *node;

    head = NULL;
    /* Rest of the code remains the same */

    (void)head;
    (void)node; // To silence the unused variable warnings

    return (0);
}

